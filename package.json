{
  "name": "yasin-laptop-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Production platform for Yasin Laptop Hub (Owner: Yasin Wahab)",
  "workspaces": [
    "backend",
    "frontend",
    "admin"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:admin": "npm run dev --workspace=admin",
    "build:backend": "npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "build:admin": "npm run build --workspace=admin",
    "build": "npm run build:backend && npm run build:frontend && npm run build:admin",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend && npm run lint --workspace=admin",
    "type-check": "npm run type-check --workspace=backend && npm run type-check --workspace=frontend && npm run type-check --workspace=admin"
  },
  "keywords": [
    "laptops",
    "yasin-laptop-hub",
    "ecommerce",
    "nextjs",
    "express",
    "mongodb"
  ],
  "author": "Yasin Wahab",
  "license": "UNLICENSED"
}
